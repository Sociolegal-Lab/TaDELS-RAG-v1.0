# 投稿區（GitHub PR）

1. 在本資料夾建立一個資料夾，名稱可自由命名：`submissions/<自訂名稱>/`
2. 放入 `predictions_test.json`（固定格式，先用根目錄 `validate_submission.py` 自驗）
3. 開 Pull Request

維護者會跑評分、更新 `leaderboard.md`。也可改用網頁 `https://handed-hesitate-rummage.ngrok-free.dev/benchmark` 直接上傳，更快。
